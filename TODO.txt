* Mouse tests.
* inline header methods in keyboard and mouse interface.
* Add getters to the mouse to stay consistent with the keyboard.
* Use _ in tests instead of camel case. See keyboard (and mouse) tests.
* Move GLFW into a root lib folder to keep solution and file layout in sync?
* Include everything math'y in Math/Math.h.
* Use alias instead of typedef?
* Camera scene objects.
** Contains viewport and projection matrix. Inverse projection as well? Probably.
** Plus Utils for creating the projection matrices.
* Modules!
* Renderer.

libs
* TinyEXR
* TinyOBJ
* OptiX

* Use Visual Studio 2015 to allow Math/Constants.h to use variable templates. (I'm looking at you PI!)